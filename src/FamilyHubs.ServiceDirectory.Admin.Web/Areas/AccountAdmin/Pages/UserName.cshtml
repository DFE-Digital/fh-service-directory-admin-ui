@page
@model UserName
@{
    ViewData["Title"] = "User Name";
    var errorCss = Model.HasValidationError ? "govuk-input--error" : string.Empty;
}

<input class="govuk-input @errorCss" id="fullName" asp-for="@Model.FullName" type="text" maxlength="255" autocomplete="off">